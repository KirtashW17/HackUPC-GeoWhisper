# Namespace for Action Cable connection and channel base classes.
module ApplicationCable
  # Abstract base for every Action Cable channel in the app.
  class Channel < ActionCable::Channel::Base
  end
end
