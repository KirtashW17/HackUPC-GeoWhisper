# Catch-all view helper module — currently empty.
#
# Every controller's views can call methods defined here. Project-specific
# helpers live in their own modules (see {FormsHelper}); reach for this one
# only for genuinely cross-cutting view utilities.
module ApplicationHelper
end
